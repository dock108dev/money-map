{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "No frontend shell or filesystem authority is granted in Slice 0.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
